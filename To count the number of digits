
#include <stdio.h>

int main() {
  int n,d=0, t=0 ;
  printf("enter the number");
  scanf("%d",&n);
  while(n>0)
  {
      t=n;
      n=n%10;
      d++;
      n=t/10;
  }
  printf("number of digitd are: %d", d);
  
     return 0;
}
